// トグルボタンの並べ方

#pragma once

// 四角と文字のまとまりを、行のどこへ置くか。
//
// 従来は行の真ん中へ置いていた。文字の長さが行ごとに違うので、
// 縦に並べたときに頭が揃わず読みづらい、という声があったため、
// 左端へ寄せる置き方も選べるようにしてある。
//
// 環境設定に持つ値なので、番号は変えないこと。
namespace ToggleAlign
{
    enum Type
    {
        Centred = 0,
        Left = 1,
        Size
    };
}
